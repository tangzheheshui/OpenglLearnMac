#version 330 core
layout (location = 0) in vec3 aPos;
layout (location = 1) in vec3 aNormal;

uniform mat4 uMvp;

out vec3 pFragPos;
out vec3 pNormal; 

void main()
{
    pFragPos = aPos;
    pNormal = aNormal;
    gl_Position = uMvp * vec4(aPos, 1.0);
}
